import { Box, Button, Flex } from "@chakra-ui/react"

const CustomButton = ({color}) => {
    return (
        <Button backgroundColor={color} color="white">
            Tesss
        </Button>
    )
}

export default CustomButton